<Page
    x:Class="Pexeso.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pexeso"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="rootGrid">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black" Offset="0"/>
                <GradientStop Color="Red" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Pivot RequestedTheme="Dark">
            <PivotItem Header="Game">
                 
                <!--StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center"-->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock x:Name="HighScore">
                            High Score: 0
                        </TextBlock>
                        <TextBlock x:Name="CurrentScore">
                            Current Score: 0
                        </TextBlock>
                    </StackPanel>
                    
                    <Grid Grid.Column="1" Grid.ColumnSpan="2" x:Name="GameGrid" Grid.RowSpan="2" Height="700" Width="700"></Grid>
                    <!-- Button needs to be placed after the grid otherwise the grid blocks it-->
                    <Button Grid.Column="0" Grid.Row="1" x:Name="GameReset" Content="Reset Game" RequestedTheme="Dark" HorizontalAlignment="Center"  VerticalAlignment="Center"  Click="GameReset_Click" />
                </Grid>
    
                <!--/-->
            </PivotItem>
            <PivotItem Header="Settings">
                
            </PivotItem>
            <PivotItem Header="Game History">
       
                    <ListView x:Name="ListView">
                        <ListView.ItemTemplate>
                         <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Date}"></TextBlock>
                                <TextBlock Text="{Binding Score}"></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
          
       
            </PivotItem>
            <PivotItem Header="About">

            </PivotItem>
        </Pivot>
   

        <!--StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
           
        </-->
    </Grid>
</Page>
